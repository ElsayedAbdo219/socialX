name: FTP Deploy

on:
  push:
    branches:
      - main  # Change this to your default branch if needed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.back.anceega.com
          username: sayed@anceega.com
          password: iVqDf1n-SX6E
          local-dir: ./  # The directory you want to upload (current directory)
          server-dir: /home/ance95.../backAnceega  # Change this to your target directory on the server
